{
  "name": "slack-clone-monorepo",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.33.2",
  "scripts": {
    "dev": "turbo dev --filter=web",
    "build": "turbo build",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "convex:dev": "pnpm --filter web convex:dev",
    "convex:seed": "pnpm --filter web convex:seed"
  },
  "devDependencies": {
    "turbo": "^2.6.1"
  }
}
